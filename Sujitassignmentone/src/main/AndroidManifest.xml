<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sujitassignmentone">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".alarm_showing"></activity>
        <activity
            android:name=".splash_with_theme"
            android:theme="@style/Splashtheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fragmentvedio3" />
        <activity android:name=".fragmentvedio2" />
        <activity android:name=".fragmentvedio1" />
        <activity android:name=".fragmentvedio" />
        <activity android:name=".fragmentmain" />
        <activity
            android:name=".sms_to_another"
            android:theme="@style/Splashtheme" />
        <activity android:name=".Signup" />
        <activity android:name=".firstone"></activity>
        <activity android:name=".MainActivity" />
        <receiver
            android:name=".Broadcastalarmreceiver"
            android:process=":remote" />
        <service
            android:name=".NotificationForJobScheduler"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>

</manifest>